name: lintercheck
on: [push]
jobs:
  linter-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Linter 
      - run: |
          pip install poetry
          make install
          make lint